<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:id="@+id/buttons_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="18dp"
        android:paddingTop="18dp">

        <Button
            android:id="@+id/btn_update"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/button_update" />

        <ToggleButton
            android:id="@+id/tb_reverse"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textOff="@string/button_reverse"
            android:textOn="@string/button_reverse" />

        <!--///[Show/Hide HeaderAdapter]-->
        <ToggleButton
            android:id="@+id/tb_show_header"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textOff="@string/hide_header"
            android:textOn="@string/show_header" />

        <!--///[isSticky]-->
        <ToggleButton
            android:id="@+id/tb_sticky"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textOff="@string/disable_sticky"
            android:textOn="@string/enable_sticky" />

    </LinearLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/buttons_container"
        android:background="#ffbef0ff"
        android:clipToPadding="true"
        android:padding="16dp"
        android:saveEnabled="false" />

</RelativeLayout>
